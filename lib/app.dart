import 'package:flutter/material.dart';
import 'screens/locations/locations.dart';

class App extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Locations());
  }
}